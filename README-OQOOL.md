# 🧠 Oqool - نظام عقول للتطوير الذكي

نظام متكامل للتطوير بمساعدة الذكاء الاصطناعي - يقرأ، يكتب، ينفذ، ويتعلم!

## 🚀 الأمر الرئيسي الواحد:

```bash
node oqool.mjs
```

**بس! مباشرة بدون أي شيء ثاني!**

---

## ✨ ماذا يعطيك؟

عند تشغيل `node oqool.mjs` تحصل على نظام كامل:

### ✅ قراءة الملفات
```
💬 أنت: اقرأ ملف package.json
🤖 الرد: [محتوى الملف بالكامل...]
```

### ✅ كتابة وتعديل الملفات
```
💬 أنت: اكتب ملف hello.py يطبع "مرحبا"
🤖 الرد: [ينشئ الملف...]
```

### ✅ تنفيذ أوامر bash
```
💬 أنت: نفذ npm test
🤖 الرد: [نتائج الاختبارات...]
```

### ✅ البحث في الملفات
```
💬 أنت: ابحث عن TODO في المشروع
🤖 الرد: [قائمة بكل TODO...]
```

### ✅ تطوير كامل
```
💬 أنت: أضف feature جديد لتصدير البيانات إلى CSV
🤖 الرد: [يخطط → يكتب الكود → يختبر → يعدل]
```

---

## 🎯 المميزات:

- ✅ **6 Tools كاملة**: read_file, write_file, execute_command, list_files, search_files, replace_in_file
- ✅ **Agent Loop ذكي**: يخطط للمهام المعقدة ويعدل حسب النتائج
- ✅ **23 Agent متخصص**: Backend, Frontend, DevOps, ML, Security, etc.
- ✅ **4 AI Providers**: Claude, Gemini, OpenAI, DeepSeek
- ✅ **Context Awareness**: يفهم المشروع ويتعلم منه
- ✅ **Learning System**: يتعلم من أخطائه ويحسن أداءه

---

## 🔧 أوامر إضافية (اختيارية):

```bash
# عرض المزودين المتاحة
node oqool.mjs providers

# اختبار النظام
node oqool.mjs test

# معلومات عن النظام
node oqool.mjs info

# تنفيذ مهمة مباشرة
node oqool.mjs agent "اقرأ ملف package.json"

# اختيار مزود معين
node oqool.mjs agent -p gemini

# العمل على مجلد معين
node oqool.mjs agent -d /path/to/project
```

---

## 📋 متطلبات التشغيل:

### 1. ملف `.env` مع API Keys:

```env
ANTHROPIC_API_KEY=sk-ant-...
GEMINI_API_KEY=...
OPENAI_API_KEY=sk-...
DEEPSEEK_API_KEY=...
```

### 2. تثبيت Dependencies:

```bash
npm install
```

---

## 🎓 أمثلة الاستخدام:

### مثال 1: إنشاء مشروع جديد
```
💬 أنت: أنشئ مشروع Node.js بـ Express و TypeScript
🤖 يقوم بـ:
  1. إنشاء package.json
  2. تثبيت dependencies
  3. إنشاء tsconfig.json
  4. كتابة server.ts
  5. إنشاء .gitignore
```

### مثال 2: إصلاح bugs
```
💬 أنت: اقرأ ملف app.js وأصلح أي أخطاء
🤖 يقوم بـ:
  1. قراءة الملف
  2. تحليل الأخطاء
  3. إصلاحها
  4. حفظ التعديلات
```

### مثال 3: كتابة tests
```
💬 أنت: اكتب tests لملف auth.js
🤖 يقوم بـ:
  1. قراءة auth.js
  2. فهم الوظائف
  3. كتابة unit tests
  4. تشغيل Tests
```

---

## 🏗️ البنية التحتية:

### Agent Loop:
```
User Request → Planning → Tool Selection → Execution → Verification → Response
              ↑                                                          ↓
              └─────────────── Learning & Improvement ←─────────────────┘
```

### Tools المتاحة:
1. **read_file** - قراءة أي ملف
2. **write_file** - كتابة/استبدال ملف
3. **execute_command** - تنفيذ bash commands
4. **list_files** - عرض الملفات في مجلد
5. **search_files** - البحث في المحتوى
6. **replace_in_file** - استبدال نص في ملف

### AI Providers:
- **Claude** - الأقوى للمهام المعقدة
- **Gemini** - سريع ورخيص
- **OpenAI** - متوازن
- **DeepSeek** - الأرخص للبرمجة

---

## 🚦 الخروج:

اكتب `exit` أو `خروج` في أي وقت للخروج.

---

## 🎉 ابدأ الآن!

```bash
node oqool.mjs
```

**سهل، قوي، ذكي!** 🚀

---

## 📚 للمزيد:

- `TEST-GUIDE.md` - دليل الاختبار
- `START-OQOOL.md` - دليل البداية السريعة
- `packages/shared/src/core/agent-client.ts` - كود Agent Loop
- `packages/shared/src/core/tools.ts` - كود Tools

---

**صُنع بـ ❤️ مع Claude, Gemini, و Oqool Team**
